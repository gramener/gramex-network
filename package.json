{
  "name": "@gramex/network",
  "version": "1.0.7",
  "description": "A layered map visual that renders choropleths and networks from TopoJSON files",
  "module": "dist/network.js",
  "browser": "dist/network.min.js",
  "scripts": {
    "watch": "esbuild network.js kpartite.js --format=esm --bundle --minify --allow-overwrite --sourcemap --outdir=dist --watch",
    "build-browser": "esbuild network.js kpartite.js --format=iife --global-name=gramex --bundle --minify --allow-overwrite --sourcemap --outdir=dist",
    "build-esm": "esbuild network.js kpartite.js --format=esm --bundle --minify --allow-overwrite --sourcemap --outdir=dist",
    "build": "npm run build-browser && npm run build-esm",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://code.gramener.com/cto/gramex-network"
  },
  "keywords": [
    "gramex",
    "network",
    "choropleth",
    "map"
  ],
  "author": "S Anand <root.node@gmail.com>",
  "contributors": [],
  "license": "AGPL-3.0-only",
  "bugs": {
    "url": "https://code.gramener.com/cto/gramex-network/-/issues"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "homepage": "https://gramener.com/gramex-network/",
  "prettier": {
    "printWidth": 120
  },
  "devDependencies": {
    "esbuild": "^0.19.2",
    "eslint": "^8.48.0",
    "eslint-plugin-html": "^7.1.0",
    "prettier": "^3.0.3"
  }
}
